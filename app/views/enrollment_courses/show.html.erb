<p id="notice"><%= notice %></p>

<p>
  <strong>Program enrollments:</strong>
  <%= @enrollment_course.program_enrollment.person.name %>
</p>

<p>
  <strong>Novanet courses:</strong>
  <%= @enrollment_course.novanet_course.name %>
</p>

<%= link_to 'Edit', edit_enrollment_course_path(@enrollment_course) %> |
<%= link_to 'Back', enrollment_courses_path %>
