<tr class = "fields">
    <!-- x icon to remove -->
    <%= f.link_to_remove(raw("<i class='icon-remove'></i>")) %>
    <!-- fields -->
    <%= f.input :school_id, :collection => School.all.order('name') %>
    <%= f.input :counselor_name %>
    <%= f.input :start_grade %>
</tr>
